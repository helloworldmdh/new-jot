export default {
    setUserEmail(state, payload){
        state.user_email = payload;
    }

    // called within a component with this.$store.commit('insert-method-name-here', payload_parameter);
};